import { StyleSheet } from 'react-native';


const viewStyles = StyleSheet.create({
    rowView: {
        flexDirection: 'row', 
        width: '100%', 
        alignItems: 'center', 
        alignContent: 'center'
    },
})

export { viewStyles }
import { Button } from '@rneui/themed';
import { View, ScrollView, Text } from 'react-native';
import { useEffect, useState } from 'react';

export default function SearchIngredients({navigation}) {
    return(
        <ScrollView>
            <Text h1>ingredints</Text>
        </ScrollView>
    )
};